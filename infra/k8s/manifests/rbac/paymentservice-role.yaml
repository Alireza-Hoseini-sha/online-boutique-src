apiVersion: rbac.authorization.k8s.io/v1
kind: Role

metadata:
  name: paymentservice-reader
  namespace: online-boutique

rules:
  - apiGroups: [""]
    resources:
      - pods
    verbs:
      - get
      - list
      - watch